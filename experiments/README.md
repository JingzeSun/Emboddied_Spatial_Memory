# Experiments

唯一活动实验合同是 [`projective_structural_latent_memory/`](projective_structural_latent_memory/README.md)。它由 D-032 建立，研究结构化视觉 latent 到持久 world nodes 的在线 binding、growth、dynamic persistence 与 versioned revision。

被取代的 `bounded_revision_validation/` 已移入 `docs/archive/pre_d032_projective_structural_latent_memory_2026-09-04/`。不得从 archive 与活动目录拼接混合实验。

正式运行不写入本目录；这里只提交合同、配置、小 fixture 和模板。raw outputs、checkpoint 与大型数据按 `outputs/README.md` 和 `data/README.md` 管理。
