# HC-008 Compute and Time Budget

- 状态：本地 M1 v1 已由 D-031 冻结；云预算仍未授权
- 最早激活：M1；目前已完成本地开发运行，正式/云预算待确认
- 建议默认：先限定两周机制实验；backbone 冻结；每方法一个小 sweep，formal 5 seeds 只给入围设置。

请填写可用 GPU/显存、每周小时、云预算、单 run 上限、总 trial 上限和停止日期：____

M1 v1 冻结选择（D-030/D-031）：先在本地完成 generator/metric/unit validation；A–E 每方法最多 6 个 validation trials，入围设置使用 5 个固定种子，单个正式 run 上限 2 小时；新宿主机 BugCheck 立即停止长 run。云预算授权为 AUD 0，任何租卡/付费存储/流量仍需单独许可。停止日期尚未指定。

预算不足时优先保留 A/B/C/E/L 与 paired cases，延期视觉规模和 active disambiguation。

## 2026-09-05 已知硬件与最新意向

- 已实测：RTX 4070 Laptop 约 8 GiB、CUDA 可用；torch 2.11.0+cu126。D-027 四方法 × 三种子的小网络训练完成；其约 66 MiB 张量峰值不代表视觉实验的显存需求。
- 用户表示可以租服务器，但未提供平台、账号、预算或购买授权；本轮没有创建实例或支出。
- 助手建议仅供估算：单卡 24 GB、32–64 GB RAM 的 Linux 按小时试用；未实测、非必要最低配置，也不是要求购买该规格。
- 白话：云 GPU 是让采集/特征提取/训练更快的工具，不会自动解决监督目标；先测一小批再算总费用。
- 租赁前需确认：渲染/驱动与模拟器版本兼容、存储与流量费用、每小时报价、试用上限、总预算和关机/持久存储方式；任何付费开通单独获得用户许可。
- 正式预算仍需填写：本轮总金额上限、可接受工期、单 run/trial 上限；不要依据玩具实验 13.83 秒推算视觉实验总工期。

日期 / 理由 / 影响：2026-09-06；M1 是轻量受控机制实验，先量本地峰值再决定服务器；不据此承诺 M2 视觉训练可在 8 GiB 完成。
